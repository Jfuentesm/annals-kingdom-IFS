export const W_SIZE = 6000;            // world edge, meters
export const N = 512;                  // heightfield grid
export const CELL = W_SIZE / N;
export const RES = 384;                // terrain render mesh resolution
export const SEA_Y = 0;
export const CHUNKS = 6;               // tree chunk grid
export const SPEEDS = [0, 0.1, 1, 5, 30, 120];   // sim-days per real second